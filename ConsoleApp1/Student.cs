using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.InteropServices;
using System.Text;
using System.Threading.Tasks;

namespace ConsoleApp1 //create student class with roll no,name,percentage ,brnach
                      //accepect the date and display details of student //
{
    internal class Student
    {
        /* Console.WriteLine("Enter Roll no = ");*//*
        private int rollno;
        private double percentage, phy, maths, chem;
        private string name, branch;

        public Student()
        {
            rollno = 0;
            phy = '';
            maths = '';
            chem = '';
            name = "";
            branch = "";
            percentage = 0;


        }
        public Student(int id,)
        {
            total = phy + maths + chem;
            percentage = total / 3;

        }
        public string PrintStudent()
        {
            return $"Student:{name}/{rollno}/{branch}/{total}/{percentage}";
        }

        public void Grade()
        {
            if (percentage > 70)
            {
                Console.WriteLine("Grade A");
            }
            else if (percentage < 50)
            {
                Console.WriteLine("GRADE B");
            }
            else
            {
                Console.WriteLine("FAIL!!!!!!");
            }*/
        }
    }

